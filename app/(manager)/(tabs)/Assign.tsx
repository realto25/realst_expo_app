// app/(manager)/(tabs)/Assign.tsx
import { ManagerVisitRequests } from "@/components/ManagerRequests";

// Make sure the component name matches the file name
export default function Assign() {  // Changed from AssignScreen to Assign
  return <ManagerVisitRequests />;
}